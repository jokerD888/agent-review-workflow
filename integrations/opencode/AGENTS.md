# ARW for OpenCode

Use the local `arw-mcp` server's typed `workflow_*` tools for ARW intents.
Before working, read `workflow_context`; for a dependent child task, explain
that its review is conditional until the parent is approved. Never treat ARW
as authorization for remote or destructive Git actions.
