# ARW for Claude Code

Read ARW task context before changing task code. Map natural-language task,
review, park, resume, and refresh requests to the corresponding structured
`workflow_*` MCP tool. Do not invoke arbitrary shell text through ARW, and do
not merge, push, rebase, reset, or delete project state.
