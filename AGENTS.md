# Repository instructions

- Keep this repository dependency-free and safe to run repeatedly.
- Preserve existing user instruction files; installers may only update their own
  marked blocks.
- Keep the Windows PowerShell and POSIX shell installers behaviorally aligned.
- Do not add credentials, personal paths, or generated machine-specific files.
- Test installer changes against a temporary home directory before committing.
